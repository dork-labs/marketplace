{
  "//": "Template for config.local.json. Copy this file to config.local.json (gitignored), fill in your values, and delete any block you don't need. Holds per-machine secrets and overrides. Precedence: env var > config.local.json > config.json > schema defaults. See .agents/flow/CONFIG.md.",
  "secrets": {
    "trackerToken": "REPLACE_WITH_YOUR_TRACKER_API_TOKEN_OR_OMIT_IF_THE_HOST_HANDLES_AUTH",
    "trackerAccount": "REPLACE_WITH_THE_CONNECTION_OR_ACCOUNT_HANDLE_THE_ADAPTER_AUTHENTICATES_THROUGH"
  },
  "identity": {
    "reviewer": "REPLACE_WITH_YOUR_TRACKER_ACCOUNT_HANDLE_FOR_REVIEW_HANDOFF"
  },
  "autonomy": {
    "default": "manual"
  }
}
