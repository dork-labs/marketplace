{
  "//": "Template for your project's .agents/flow/config.local.json (ignored by git). /flow:init copies it there; fill in your values and delete any block you don't need. Holds this machine's secrets and overrides. Precedence: env var > config.local.json > config.json > schema defaults. See CONFIG.md beside this file in the flow plugin.",
  "secrets": {
    "trackerToken": "REPLACE_WITH_YOUR_TRACKER_API_TOKEN_OR_OMIT_IF_THE_HOST_HANDLES_AUTH",
    "trackerAccount": "REPLACE_WITH_THE_CONNECTION_OR_ACCOUNT_HANDLE_THE_ADAPTER_AUTHENTICATES_THROUGH"
  },
  "connection": {
    "team": {
      "key": "REPLACE_WITH_YOUR_TEAM_KEY_OR_ITEM_PREFIX",
      "id": "REPLACE_WITH_YOUR_TEAM_ID"
    },
    "workspace": {
      "slug": "REPLACE_WITH_YOUR_WORKSPACE_OR_ORG_SLUG"
    }
  },
  "identity": {
    "reviewer": "REPLACE_WITH_YOUR_TRACKER_ACCOUNT_HANDLE_FOR_REVIEW_HANDOFF"
  },
  "models": {
    "bindings": {
      "workhorse": "<your-strong-general-model>",
      "fast": "<your-cheap-fast-model>"
    }
  },
  "autonomy": {
    "default": "manual"
  }
}
